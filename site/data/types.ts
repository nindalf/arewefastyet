
export enum CompilerMode {
    Check = 'Check',
    Debug = 'Debug',
    Release = 'Release',
}

export enum ProfileMode {
    Clean = 'Clean',
    Incremental = 'Incremental',
}
export enum System {
    TwoCores = '2 cores',
    FourCores = '4 cores',
    EightCores = '8 cores',
    SixteenCores = '16 cores',
}